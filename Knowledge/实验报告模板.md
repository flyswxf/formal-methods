# 【形式化课程实验报告】

*标题改为你的报告名称

**报告题目：** 

**姓名：** 

**学号：** 

## 1. 实验背景与目的

- **目标：** 实现一个支持 DIMACS 格式输入的完整 CDCLSAT 求解器。
- **要求：** 能够正确输出命题公式的满足性（SAT/UNSAT），并且在结果为 SAT 时输出对应的变量赋值解。
- **对比：** 通过与工业级求解器 Minisat (C++版) 在大规模测试集（>1000个用例）上的对比，评估所实现求解器的正确性与性能。

## 2. 求解器系统设计与实现

### 2.1 系统总体架构

- 简述代码的模块划分（例如：解析器、核心求解引擎、启发式分支策略、子句学习模块、重启机制等）。

### 2.2 I/O 模块实现

- **输入解析：** 说明如何读取并解析标准的 DIMACS CNF 文件格式。
- **输出格式：** 说明如何格式化输出 `s SATISFIABLE` / `s UNSATISFIABLE`，以及 `v ...` （变量赋值序列）。

### 2.3 核心算法 (CDCL) 实现细节

## 3. 大模型辅助开发过程

- **代码生成：** 大模型在哪些具体模块（如 DIMACS 解析器、基础数据结构构建）提供了代码原型。
- **Debug 与问题解决：** 在实现核心 CDCL 逻辑（如 1UIP 寻找或内存泄漏）时，大模型如何协助定位和修复 Bug。
- **自动化脚本：** 是否利用大模型编写了批量测试脚本或数据统计脚本。

## 4. 实验设置

### 4.1 实验运行环境

- **操作系统：** 
- **编程语言与编译器：** 
- **硬件配置：** 

### 4.2 测试数据集

- **数据来源：** 往年 SAT Competition 的公开测试集。
- **数据规模：** 详细说明选取的实例总数（需 $\ge 1000$ 个），以及它们的变量规模和子句规模分布（可使用图表展示）。

### 4.3 比较基准

- **基准工具：** Minisat (C++ 版本)。
- **测试指标：** 主要对比**正确性**（你的求解器与 Minisat 结果是否一致）和**性能**（求解耗时，Time-out 设置为多少秒）。

## 5. 实验结果与对比分析

### 5.1 正确性验证

- **验证方法：** 将自研求解器的结果与 Minisat 逐一比对。对于输出 SAT 的情况，验证输出的解是否真正满足原 DIMACS 文件中的所有子句。
- **结果统计：** 测试集总数 1000+ 个，正确匹配的个数、失败/超时的个数。（建议用表格展示）

### 5.2 性能评估 (时间开销)

- **耗时对比：** 列出你的求解器与 Minisat 在整体测试集上的平均耗时、中位数耗时。
- **性能瓶颈分析：** 如果你的求解器比 Minisat 慢，深入分析原因

**此处建议使用图片进行对比**

## 6. 总结

- 总结本次实验的完成情况，达到了哪些目标。
- 提出未来可以继续优化求解器的方向。
